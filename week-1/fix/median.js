// Fix this implementation
// Start by running the tests for this function
// If you're in the week-1 directory, you can run npm test -- fix to run the tests in the fix directory

function calculateMedian(list) {

  //1. Sort the numbers in order

  //2.if the number of numbers is odd number you the middle number is the median
  const arr = arr.sort(function (a,b) {return a -b })
  return [0];
    if (arr.length % 2=== 0){
      return  (arr [middleIndex -1]  + arr[middleIndex   ] )/2; 
      
    }
    else return
     arr [middleIndex];
    }
    module.exports = calculateMedian;
    console.log(calculateMedian(2,3,6,8));
    
    
  l
  //3.if the number of numbers is even number,  you add two middle numbers and divide them by 2

  // const arr = list.sort((x, y) => x - y)

  // const middleIndex = Math.floor(arr.length / 2);
  // let median = list[middleIndex];

  // if (arr.length % 2 == 0){
  //   return median

  //   }
  
  console.log(calculateMedian([3, 6, 8, 2]));


module.exports=calculateMedian;







